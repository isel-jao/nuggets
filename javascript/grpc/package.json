{
  "scripts": {
    "postinstall": "buf mod update",
    "clean": "git clean -fdx",
    "generate": "buf generate",
    "dev:server": "tsx --watch src/server.ts",
    "dev:client": "tsx --watch src/client.ts",
    "build": "tsc --build tsconfig.json",
    "prebuild": "npm run generate"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "@types/node": "^26.1.1",
    "ts-proto": "^2.12.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.13.0",
    "@grpc/grpc-js": "^1.14.4"
  }
}
