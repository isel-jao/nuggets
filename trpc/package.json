{
  "name": "trpc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server:dev": "npx nodemon --watch server --exec 'clear && npx ts-node server'",
    "client:dev": "npx nodemon --watch client --exec 'clear && npx ts-node client'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.15.0",
    "@trpc/client": "^10.29.1",
    "@trpc/server": "^10.29.1",
    "@types/jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.1.4",
    "jsonwebtoken": "^9.0.0",
    "redis": "^4.6.7",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "prisma": "^4.15.0"
  }
}
