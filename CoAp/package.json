{
  "name": "coap",
  "version": "1.0.0",
  "description": "## Descreption",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "server:dev": "npx nodemon --watch 'server/**/*' --exec 'clear && node server/main.js'",
    "client:dev": "npx nodemon --watch 'client/**/*' --exec 'clear && node client/main.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "coap": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
